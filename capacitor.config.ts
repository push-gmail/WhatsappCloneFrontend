import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'whatsapp.aggr.co.in',
  appName: 'whatsapp-clone-frontend',
  webDir: 'dist'
};

export default config;
